// node - opens a REPL (READ-EVALUATE-PRINT-LOOP) where you can write javascript code

// this can be executed in the terminal using "node firstScript.js" (make sure you are in the right path with the bash)
for (let i = 0; i < 10; i++) {
    console.log("HELLO");
}
